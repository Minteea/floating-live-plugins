{
  "name": "@floating-live/platform",
  "version": "0.16.0",
  "type": "module",
  "main": "index.js",
  "types": "index.d.ts",
  "author": "Minteea",
  "license": "MIT",
  "scripts": {
    "check": "tsc --noEmit",
    "clear": "rimraf *.js *.d.ts --glob",
    "build": "npm run clear && tsc --build",
    "prepack": "npm run build",
    "postpack": "npm run clear"
  },
  "devDependencies": {
    "rimraf": "^3.0.2",
    "typescript": "^5.8.3"
  },
  "peerDependencies": {
    "floating-live": "^0.16.0"
  }
}